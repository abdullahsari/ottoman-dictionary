////////////////////////////////////////////////////////////////////////////////
// Removes duplicates (only primitive types) from a given array
////////////////////////////////////////////////////////////////////////////////
export const uniquify = array => {
    return array.filter((el, i, a) => i === a.indexOf(el));
};
